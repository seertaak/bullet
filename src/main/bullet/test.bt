var f: fn (x): x concat "foobar"

print: 200 toString | f

var DEBUG false
for (var i: + 5 0; < i 10; set i: + i 1)
  cond 
    DEBUG ; print i
    true: 0

print: "martin" toUpperCase -> substring 1 3 -> substring 0
set x: * 4: * 2 1024
//var f: fn (x): + x 200
set x: * x 4
var name "Martin"
print name.toUpperCase.toString name.toUpperCase.toString.length
print: f x
print x

set x: "martin" toUpperCase -> substring 1 3 | val
set x: x toLowerCase
print x

